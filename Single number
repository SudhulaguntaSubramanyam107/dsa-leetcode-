var singleNumber = function(nums) {
    freq = {}
    for (let num of nums){
        if (freq[num]){
            freq[num]++
        } else {
            freq[num] =1
        }
    }
    for (let i = 0; i<nums.length; i++){
        if (freq[nums[i]]==1){
            return nums[i]
        }
    }   
    return -1 
};
